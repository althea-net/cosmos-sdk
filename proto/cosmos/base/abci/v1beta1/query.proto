syntax = "proto3";
package cosmos.bank.v1beta1;

import "tendermint/abci/types.proto";

// ApplicationQueryService exposes the ABCIApplication Query method on BaseApp
service ApplicationQueryService {
  // Query exposes the ABCIApplication Query method on BaseApp
  rpc Query(tendermint.abci.RequestQuery) returns (tendermint.abci.ResponseQuery);
}